public class Member {
	public String id;
	public Member(String id) {
		this.id = id;
	}
	
	@Override
	
public boolean equals(Object obj) {
		if(obj instanceof Member) {
			Member membeer = (Member) obj;
			if(id,equals(member.)) {
				return.true;
				}
			}
		return false;
	}
}